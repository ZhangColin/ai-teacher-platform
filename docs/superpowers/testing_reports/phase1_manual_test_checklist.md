# 第一阶段手动测试清单

**测试日期**: 2026-04-04
**测试人员**: [填写]
**浏览器**: [填写]

---

## 测试前准备

### 环境检查
- [ ] 后端服务运行在 http://localhost:8000
- [ ] 前端服务运行在 http://localhost:5173
- [ ] 已配置至少一个AI模型（DeepSeek/Kimi/OpenAI）
- [ ] 数据库连接正常

### 启动命令
```bash
# 终端1：启动后端
cd backend
python3 -m src.main

# 终端2：启动前端
cd frontend
npm run dev
```

---

## 测试场景

### 场景1：生成简单代码块

**目的**: 验证代码块标记不重复

**步骤**:
1. 登录系统
2. 选择"HTML生成助手"工具（或任意代码生成工具）
3. 发送消息："生成一个简单的导航栏HTML代码，包含logo和3个链接"
4. 观察流式输出过程

**预期结果**:
- [ ] 代码块正确显示，无重复的 ``` 标记
- [ ] Markdown渲染正常
- [ ] HTML代码可以复制使用
- [ ] 最终内容格式：单个 ```html 开头，单个 ``` 结尾

**实际结果**: [填写]

**问题记录**: [填写]

---

### 场景2：长内容触发续写

**目的**: 验证续写时无重复代码块标记

**步骤**:
1. 发送消息："生成一个完整的电商首页HTML，包含：导航栏、轮播图、产品列表（4个产品）、页脚信息。每个产品包括图片、标题、价格、购买按钮。使用现代CSS样式。"
2. 观察是否触发续写（后端日志会显示"内容不完整，触发续写"）
3. 检查最终拼接的内容

**预期结果**:
- [ ] 续写时无重复代码块标记
- [ ] 最终内容完整可执行
- [ ] 用户无感知延迟
- [ ] 前端日志显示"🔧 检测到续写内容重复"（如果有重复）

**实际结果**: [填写]

**问题记录**: [填写]

---

### 场景3：多语言代码块

**目的**: 验证不同编程语言的代码块都能正确处理

**步骤**:
1. 依次测试以下语言：
   - Python: "生成一个Python函数，计算斐波那契数列"
   - JavaScript: "生成一个JavaScript函数，实现深拷贝"
   - Java: "生成一个Java类，实现单例模式"
   - SQL: "生成一个SQL查询，连接两个表"

**预期结果**:
- [ ] 所有语言的代码块都正确显示
- [ ] 无重复的代码块标记
- [ ] 代码高亮正确

**实际结果**: [填写]

**问题记录**: [填写]

---

### 场景4：HTML标签完整性

**目的**: 验证HTML标签闭合检测

**步骤**:
1. 发送消息："生成一个HTML表格，包含5行3列，带斑马纹样式"
2. 查看后端日志，观察是否进行完整性验证
3. 检查生成的HTML是否闭合

**预期结果**:
- [ ] HTML标签正确闭合
- [ ] 后端日志显示"内容验证通过"或"内容不完整，触发续写"
- [ ] 生成的HTML可以正确渲染

**实际结果**: [填写]

**问题记录**: [填写]

---

### 场景5：括号配对检测

**目的**: 验证括号平衡检测

**步骤**:
1. 发送消息："生成一个JavaScript函数，包含多层嵌套的对象和数组"
2. 检查生成的内容

**预期结果**:
- [ ] 所有括号正确配对：`{}`, `[]`, `()`
- [ ] 代码可以执行
- [ ] 后端日志显示验证结果

**实际结果**: [填写]

**问题记录**: [填写]

---

### 场景6：短内容不触发续写

**目的**: 验证完整内容不会误触发续写

**步骤**:
1. 发送消息："生成一个简单的段落标签"
2. 查看后端日志

**预期结果**:
- [ ] 后端日志显示"内容已完整，无需续写"
- [ ] 没有触发续写
- [ ] 内容正常返回

**实际结果**: [填写]

**问题记录**: [填写]

---

### 场景7：多模型验证

**目的**: 验证方案与模型无关

**步骤**:
使用以下模型重复场景1和场景2：
- [ ] DeepSeek
- [ ] Kimi
- [ ] OpenAI（如果可用）

**预期结果**:
- [ ] 所有模型表现一致
- [ ] 代码块标记无重复
- [ ] 续写逻辑正常工作

**实际结果**: [填写]

**问题记录**: [填写]

---

## 测试数据记录

### 代码块标记统计
| 场景 | 代码块标记数 | 是否成对 | 续写次数 | 去重次数 |
|------|-------------|----------|----------|----------|
| 场景1 | [填写] | [填写] | [填写] | [填写] |
| 场景2 | [填写] | [填写] | [填写] | [填写] |
| 场景3 | [填写] | [填写] | [填写] | [填写] |

### 性能观察
| 场景 | 流式输出时长 | 续写延迟 | 用户感知 |
|------|-------------|----------|----------|
| 场景1 | [填写] | [填写] | [填写] |
| 场景2 | [填写] | [填写] | [填写] |

---

## 发现的问题汇总

### 严重问题（阻断发布）
- [ ] 问题描述：
  - 复现步骤：
  - 实际结果：
  - 期望结果：

### 中等问题（影响体验）
- [ ] 问题描述：

### 轻微问题（可以后续优化）
- [ ] 问题描述：

---

## 总体评价

### 功能完整性
- [ ] 通过 / 不通过

### 性能表现
- [ ] 通过 / 不通过

### 用户体验
- [ ] 通过 / 不通过

### 最终结论
- [ ] **通过** - 可以发布
- [ ] **不通过** - 需要修复问题

**备注**: [填写]

---

## 测试签名

**测试人员**: ________________  日期: ________

**审核人员**: ________________  日期: ________
