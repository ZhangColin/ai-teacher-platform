# 架构设计待办列表 (TODO List)

> **文档定位**: 记录架构设计阶段的 P1（可假设）和 P2（后续迭代）问题
> **更新机制**: 完成设计工作后更新，处理时标记为"已处理"或删除

---

## P1 假设与处理方式

> **说明**: P1 问题已在设计文档中采用假设方案，以下记录问题、当前处理方式、可能方案。

| ID | 问题 | 当前处理方式 | 可能方案 | 状态 | 创建时间 |
|:---|:---|:---|:---|:---|:---|
| P1-001 | 会话历史传递机制 | 前端在每次请求时传递完整历史消息列表，后端仅透传给AI服务，不存储。接口设计兼容未来扩展（history参数可选） | 方案A：前端传递历史消息，后端仅透传（当前采用）<br>方案B：后端完整实现会话存储和管理，前端仅传递session_id | 待处理 | 2026-01-01 |

---

## P2 后续迭代考虑

> **说明**: P2 问题未在设计文档中体现，以下记录问题、可能方案。

| ID | 问题 | 可能方案 | 状态 | 创建时间 |
|:---|:---|:---|:---|:---|
| P2-001 | 代码块嵌套解析优化 | 方案A：只解析最外层代码块<br>方案B：递归解析所有代码块 | 待处理 | 2026-01-01 |
| P2-002 | 欢迎语生成的异步优化 | 方案A：当前同步生成，后续支持异步<br>方案B：直接支持异步生成 | 待处理 | 2026-01-01 |
| P2-003 | 配置验证规则的详细细化 | 方案A：当前基础验证，后续细化<br>方案B：当前就详细验证 | 待处理 | 2026-01-01 |

---

**最后更新**: 2026-01-01

---

## 设计说明

### P1 假设与处理方式
- **P1-001 会话历史传递机制**：当前采用前端传递历史消息，后端仅透传的方案。接口设计兼容未来扩展（`history` 参数可选），未来如需后端存储，只需增加存储逻辑，前端逐步迁移即可。

### P2 后续迭代考虑
- **P2-001 代码块嵌套解析优化**：当前仅解析最外层代码块，嵌套代码块的处理留待后续迭代优化。
- **P2-002 欢迎语生成的异步优化**：当前同步生成，如遇性能问题，后续可扩展异步生成。
- **P2-003 配置验证规则的详细细化**：当前基础验证（必填字段检查），详细验证规则留待后续迭代细化。

