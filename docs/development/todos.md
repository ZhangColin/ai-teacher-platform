# 开发待办列表 (Development Todos)

> **文档定位**: 记录开发阶段的 P1（可假设）和 P2（后续迭代）问题
> **更新机制**: 完成开发工作后更新，处理时标记为"已处理"或删除

---

## P1 假设与处理方式

> **说明**: P1 问题已在设计文档中采用假设方案，以下记录问题、当前处理方式、可能方案。

| ID | 问题 | 当前处理方式 | 可能方案 | 状态 | 创建时间 |
|:---|:---|:---|:---|:---|:---|
| - | - | - | - | - | - |

**说明**：P1 问题（代码块语言标识处理、会话命名长度、工具分类）按用户要求，不做待办，具体问题具体处理。

---

## P2 后续迭代考虑

> **说明**: P2 问题不影响 MVP，后续迭代处理。

| ID | 问题 | 可能方案 | 状态 | 创建时间 |
|:---|:---|:---|:---|:---|
| P2-001 | 代码块嵌套解析优化 | 方案A：只解析最外层代码块<br>方案B：递归解析所有代码块 | 待处理 | 2026-01-01 |
| P2-002 | 历史对话列表预览 | 方案A：显示首条消息预览<br>方案B：显示最后一条消息预览<br>方案C：不显示预览 | 待处理 | 2026-01-03 |
| P2-003 | 配置验证规则的详细细化 | 方案A：当前基础验证，后续细化<br>方案B：当前就详细验证 | 待处理 | 2026-01-01 |
| P2-004 | 会话标题 AI 生成 | 方案A：基于第一条消息的前 N 个字符（当前采用）<br>方案B：AI 生成标题（需要额外调用） | 待处理 | 2026-01-03 |
| P2-005 | 成果物存储优化 | 方案A：当前使用数据库存储<br>方案B：使用对象存储或文件系统（大文本内容） | 待处理 | 2026-01-03 |
| P2-006 | 流式输出显示优化 | 问题：虽然调用了流式接口，但前端显示还是一下子出来<br>可能原因：后端返回太快、前端响应式更新问题、数据缓冲<br>需要排查：检查 SSE 数据接收、Vue 响应式更新、浏览器缓冲 | 待处理 | 2026-01-03 |
| P2-007 | 历史列表收起时消息区域左边距调整 | 问题：当历史对话列表收起后，收起按钮会压到消息文字<br>当前尝试：使用内联样式设置 paddingLeft: '80px' 未生效<br>需要排查：样式优先级问题、Tailwind px-6 覆盖问题、实现方式 | 待处理 | 2026-01-03 |

---

**最后更新**: 2026-01-03（添加历史列表收起时消息区域左边距调整问题）

---

## 讨论确认记录

### 2026-01-03 讨论确认

**P0 问题讨论结果**：

1. **欢迎语处理**：使用配置的 `welcome_message`，AI 返回欢迎语的情况暂时不考虑（当没这事）
2. **预览功能范围**：MVP 阶段实现基础预览（Markdown 渲染、HTML/SVG 沙箱执行），全屏/下载/截图等功能拆成开发任务，标记为低优先级（P2），不需要再走需求-设计流程
3. **会话持久化**：采用前后端协作方式（后端存储，前端调用 API 恢复），已更新需求文档
4. **登录跳转逻辑**：已实现，无需讨论

**P1 问题处理方式**：
- P1-001（代码块语言标识处理）：不做待办，具体问题具体处理
- P1-002（会话命名长度）：不做待办，按 UI 显示情况拍个长度，不好当场调整
- P1-003（工具分类）：不做待办，暂时配置够用
